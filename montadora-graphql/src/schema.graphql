type Montadora {
  id: ID!
  nome: String
  veiculo:[Veiculo]!
}


type Veiculo {
  id: ID!
  nome: String
  cor: String
  modelo: String
  montadora: Montadora!
}


type Query {
  montadoras: [Montadora!]!
  veiculos: [Veiculo!]!
}

type Mutation {
  cadastrarMontadora(nome:String): Montadora!
  cadastrarVeiculo(cor:String, modelo:String, montadoraId:ID!): Veiculo!
}

type Mutation {
  removerMontadora(nome:String): Montadora!
  removerVeiculo(nome:String, montadoraId:ID!): Veiculo!
}